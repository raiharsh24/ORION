{
  "name": "orion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=desktop\" \"npm run dev --workspace=gateway\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}